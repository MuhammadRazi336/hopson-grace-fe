/**
 * Sub-collection deep link: same UI as `products.$handle` but lives at
 * `/products/subcollection/:handle` so header swiper clicks can navigate
 * here instead of mutating state in place.
 */
export {loader, action, meta, handle} from './products.$handle.jsx';
export {default} from './products.$handle.jsx';
